import 'package:flutter/material.dart';

class PokemonListMobile extends StatelessWidget {
  const PokemonListMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PokemonListMobile')),
    );
  }
}
